import React from 'react'

const TechStack = ((props, ref) => {
  return (
    <div ref={ref} className="techStack section">
      <section id="teckStack" className="techStack section">
        techstack
      </section>
    </div>
  );
})

export default TechStack
